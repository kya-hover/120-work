// Declare the setup function
function setup() {

  // create a canvas 800px wide and 600px high
  createCanvas(800, 600);

  // color the background 'green'
  background('green')
}

// Declare the draw function
function draw(){
  /* tell p5 to print the text string "Hello World!"
  This is goimg to be positioned at (x: 375, y: 300). */
  text("Hello World!", 375, 300);
}
